{
  "name": "easy-shortcut-key",
  "version": "0.1.0",
  "description": "Easy Shortcut Key - desktop wrapper",
  "main": "electron-main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . EasyShortcutKey --platform=darwin --arch=x64 --out=dist --overwrite"
  },
  "devDependencies": {
    "electron": "^25.0.0",
    "electron-packager": "^17.1.0"
  }
}
